package net.minecraftforge.client;

/**
 * STUB. The original {@code GuiControlsScrollPanel} extends vanilla
 * {@code GuiSlot} which MITE made abstract with a different method shape
 * (added abstract drawBackground; renamed renderEngine accessors).
 * Replacing it with an empty class lets Forge mods that reference the
 * type by name compile; the actual key-binding scroll panel stays in
 * vanilla form for now.
 */
public class GuiControlsScrollPanel {
    // Intentionally empty — see class javadoc.
}
