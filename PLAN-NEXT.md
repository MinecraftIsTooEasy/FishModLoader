# 下一阶段计划：真实 Forge mod 兼容矩阵

状态：待执行
前置基线：[`PLAN.md`](PLAN.md) 中的 forge-compat 基础能力和聚合验证均通过。

## 目标与范围

以**可合法取得、面向 Minecraft/Forge 1.6.4 的真实第三方 mod jar**为样本，建立可复现、可审计的客户端/服务端兼容矩阵。对每个样本记录来源、版本、配置、执行证据和失败阶段，并按共性问题推动兼容层修复。

范围包括：

- mod 发现、依赖解析、构造与 Forge 生命周期；
- Access Transformer、事件总线、网络通道、配置、资源与常见注册 API；
- 客户端启动/进服和独立服务端启动/停服；
- 单 mod 隔离测试，以及已通过样本的小规模组合回归；
- 由真实样本暴露的问题最小化、分类和自动回归化。

## 非目标

- 不承诺所有 Forge 1.6.4 mod 无条件兼容。
- 不虚构、捆绑或重新分发许可证不允许入库的 mod jar；默认只记录获取方式和校验值。
- 不把内容玩法完整通关、性能调优、跨 Minecraft 版本兼容作为本阶段验收条件。
- 不以修改第三方 mod 二进制作为常规方案；必要的临时诊断 patch 必须单独记录，不能计为原样兼容通过。
- 不用“能到主菜单”替代服务端、联网或功能路径验收。

## 样本选择与类别

先选取体量小、依赖清晰、来源可追溯的样本，再扩展复杂度。下列是**待覆盖类别，不代表已有任何真实 mod 通过**：

1. **基础生命周期**：仅使用 `@Mod`、preInit/init/postInit 和服务端生命周期。
2. **注册与事件**：方块、物品、配方、事件总线、tick/玩家事件。
3. **配置与持久化**：Forge Configuration、世界级数据或自定义保存处理。
4. **Access Transformer / 反射**：自带 AT、延迟类加载、访问私有或 final 成员。
5. **网络**：legacy packet/channel、客户端与服务端握手、消息收发。
6. **客户端资源与渲染**：纹理、语言、GUI、渲染或按键绑定；标记为 client-required。
7. **服务端专用/无客户端内容**：独服可启动并完成核心路径。
8. **依赖关系**：一个前置库加一个消费 mod，验证依赖排序与缺失依赖报告。
9. **核心修改型样本**：coremod/transformer 类能力仅作后期探索项，必须与普通 mod 结果分栏，不阻塞前述矩阵。

## 优先级

### P0：真实发布 mod 的命名空间重映射

- **当前首要 blocker**：真实 Lucky Block 4.2.1 发布 jar 使用 official/Notch 游戏名（例如 `aqz`），而运行时游戏 jar 使用 intermediary。现有 `ForgeSrgModRemapper` identity 假设已被 `NoClassDefFoundError: aqz` 实证推翻；实现计划见 [`docs/forge-mod-remap-plan.md`](docs/forge-mod-remap-plan.md)。完成该计划前，不能把“发现到 jar”或“客户端能进存档”记为 mod 兼容。
- 完成 official → intermediary 的 Forge mod JAR 级预重映射、命名空间判定、缓存和 fail-closed 行为，并用仓库自建 fixture 自动回归；不得提交或分发第三方 Lucky Block jar。
- Lucky Block 4.2.1 当前真实客户端状态为 **FAIL**：official remap、构造、active list、preInit/init/postInit 和注册均已走通，但人工验收中方块为紫黑缺失纹理，放置后敲掉只掉落本体，未触发幸运行为。详见 [`docs/lucky-block-compat-handoff.md`](docs/lucky-block-compat-handoff.md)。修复并完成新世界功能路径前不得记为 PASS。
- 随后审计当前真实 AT 服务端日志中的非致命 `InvalidMixinException` warning，修复或禁用无效 patch；若确属可接受噪声，则建立带原因的允许清单和回归断言。完成前不得把静态“缺失 0”等同于运行时 Mixin 全部生效。
- 定义样本清单格式、许可证/来源规则、SHA-256 和本地放置约定。
- 选择少量基础生命周期、注册/事件、配置类别样本。
- 每个样本先单独验证，建立客户端和服务端证据记录。
- 将可稳定自动化的启动、日志断言和停服流程纳入 Gradle 或独立脚本。

### P1：常见 Forge 能力

- 扩展到 AT、网络、持久化、客户端资源与一个明确依赖链。
- 对失败按 discovery / classloading / transform / lifecycle / registry / network / gameplay 分类。
- 每次兼容修复新增最小夹具或真实样本回归断言，避免只靠人工日志判断。

### P2：组合与高风险能力

- 组合运行已单独通过的样本，检查事件、ID、资源和 classloader 冲突。
- 探索 coremod/额外 transformer 等高风险类别；单独陈述支持边界。
- 评估是否形成可公开维护的支持等级，而不是简单二元“兼容/不兼容”。

## 验收方式

### 所有样本共同前置

- 记录 mod 名称、精确版本、下载来源、许可证信息和 jar SHA-256。
- 记录 FishModLoader commit、JDK、MITE jar 身份/校验值、操作系统、客户端/服务端配置。
- 先运行 `./gradlew verifyForgeCompatibilityQuick`；基础回归失败时不进入真实 mod 判定。
- 保留完整日志，禁止只摘录成功片段而丢弃异常上下文。

### 客户端验收

按样本能力选择并明确记录：

1. 客户端进程成功启动到主菜单，无 loader/mixin/链接错误或崩溃报告。
2. mod 被发现并完成适用的 preInit/init/postInit；日志中无被吞掉的 handler 异常。
3. 创建或载入测试世界；需要联网的样本连接对应测试服务端。
4. 执行该 mod 的一个最小可观察功能路径（例如注册内容可见、GUI 可打开、事件有效或网络消息往返）。
5. 正常退出并保存客户端日志、崩溃目录检查结果和必要截图/步骤。

仅适用于服务端的 mod 可将客户端标记为 `N/A`，必须写明依据，不能记为通过。

### 服务端验收

1. 使用 `*-all-intermediary.jar` 启动独立服务端，加载被测 mod。
2. mod 完成适用生命周期，服务端日志出现 `Done`。
3. 执行一个最小可观察服务端功能路径；网络类样本至少完成一次客户端连接及消息路径。
4. 发送 `stop` 后在时限内以退出码 0 结束，无未处理异常、崩溃报告或数据保存错误。
5. 保存完整服务端日志及生成世界/配置的必要摘要。

纯客户端 mod 的服务端结果可标记为 `N/A`；若其声明或预期能在服务端忽略，则还应验证不会导致独服崩溃。

## 结果状态

每个“样本 × 版本 × 环境 × 侧”只能使用以下状态之一：

- `PASS`：全部适用验收项有证据。
- `PARTIAL`：可启动但功能路径或某一适用侧失败；必须列出边界。
- `FAIL`：存在可复现阻塞错误。
- `BLOCKED`：缺少合法样本、依赖、环境或可执行前置，尚不能判定。
- `N/A`：该侧明确不适用，并有依据。
- `NOT TESTED`：尚未执行；不得写成通过。

## 证据记录模板

建议每个样本建立一份记录，或在统一矩阵中链接到详细记录：

```markdown
### <mod 名称> <版本>

- 状态：NOT TESTED
- 类别：
- 来源 URL：
- 许可证/再分发约束：
- mod jar SHA-256：
- 依赖及版本：
- FishModLoader commit：
- MITE jar SHA-256：
- JDK / OS：
- 配置与启动参数：

#### 客户端
- 结果：NOT TESTED
- 执行命令/脚本：
- 验收步骤：
- 退出码：
- 决定性日志：
- 完整日志路径：
- 崩溃报告/截图：

#### 服务端
- 结果：NOT TESTED
- 执行命令/脚本：
- 验收步骤：
- 退出码：
- 决定性日志：
- 完整日志路径：

#### 结论与问题分类
- 首个失败阶段：
- 最小复现：
- 是否需要 loader 修复：
- 自动回归入口：
- 已知限制/后续：
```

## 阶段退出条件

本阶段只有在以下条件全部满足后才能标记完成：

- 已建立版本化矩阵和逐样本证据记录，所有条目都有来源与 SHA-256。
- P0 类别均至少有一个**真实样本**完成判定；不能用仓库自带夹具替代真实 mod。
- 至少覆盖一个客户端功能样本和一个可在独立服务端执行功能路径的样本；两者可不是同一个 mod。
- 所有计为 `PASS` 的样本同时满足其适用侧验收，且能从记录复现。
- 所有为兼容真实样本而修改的 loader 行为都有自动化回归；`verifyForgeCompatibilityQuick` 与 `verifyForgeCompatibilityIntegration` 继续通过。
- `PARTIAL`、`FAIL`、`BLOCKED` 和 coremod 支持边界均被明确记录，没有把未测试项描述成已支持。
- 当 `PASS` 样本达到 2 个或以上时，必须执行至少一组包含所有已通过样本的组合回归；仅有 1 个 `PASS` 样本时记录暂不可组合，并在第二个样本通过后补测，不能以“已记录阻塞”永久跳过组合验收。
